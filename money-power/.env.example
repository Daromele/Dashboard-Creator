DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/moneypower?schema=public"
AUTH_SECRET="generate-with-openssl-rand-hex-32"
APP_URL="http://localhost:3000"
# Optional: when set, magic links are emailed instead of returned to the browser.
# SMTP_URL="smtp://user:pass@smtp.example.com:587"
# MAIL_FROM="Money Power <no-reply@example.com>"
